name: ItemsAdderAdditions
version: '${version}'
main: toutouchien.itemsadderadditions.ItemsAdderAdditions
loader: toutouchien.itemsadderadditions.ItemsAdderAdditionsLoader
bootstrapper: toutouchien.itemsadderadditions.ItemsAdderAdditionsBootstrap
api-version: '${minMinecraftVersion}'
authors: [ PuppyTransGirl ]

dependencies:
  server:
    ItemsAdder:
      load: AFTER
      required: true

    PlaceholderAPI:
      load: BEFORE
      required: false

    CoreProtect:
      load: BEFORE
      required: false
